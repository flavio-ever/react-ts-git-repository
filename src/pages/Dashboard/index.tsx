import React from 'react';

/**
 * Este formato 'e mais interessante, pois consigo passar
 * o formato da variavel (funcao) do que usar function xxx() {}
 */
const Dashboard: React.FC = () => {
  return <h1>Dashboard</h1>;
};

export default Dashboard;
